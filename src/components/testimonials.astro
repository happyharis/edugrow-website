---
const testimonials = [
  {
    name: "Deric Yee, Founder of Sigma School",
    quote: "Been a pleasure working with Haris. He helped me understand what it means to work with a rockstar.",
    image: "/deric-yee.jpg",
  },
];
---

<div class="py-16">
  <h2 class="text-4xl font-bold mb-12 text-center">
    What our clients say
  </h2>
  
  <div class="space-y-12">
    {testimonials.map((testimonial) => (
      <div class="flex items-center gap-6 max-w-2xl mx-auto">
        <img
          src={testimonial.image}
          alt={testimonial.name}
          class="w-20 h-20 rounded-full object-cover flex-shrink-0"
        />
        <div>
          <blockquote class="text-lg text-gray-700 mb-2">
            "{testimonial.quote}"
          </blockquote>
          <cite class="text-gray-900 font-medium not-italic">
            {testimonial.name}
          </cite>
        </div>
      </div>
    ))}
  </div>
</div>